# Copie este arquivo para .env e preencha os valores

# String de conexão do MongoDB
# Opção gratuita: crie um cluster em https://mongodb.com/atlas
MONGO_URI=mongodb+srv://DracoForge_Life:ambrosio9@dracoforge.afnlrb9.mongodb.net/studyquest?retryWrites=true&w=majority
# Segredo para assinar os tokens JWT (use uma string longa e aleatória)
# Exemplo: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=simeusouomrsteve

# Porta do servidor (opcional, padrão: 3001)
PORT=3001
